version: "3.9"

services:
  jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/work
    working_dir: /work
    environment:
      JUPYTER_ENABLE_LAB: "yes"
